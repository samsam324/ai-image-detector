Put trained artifacts here.

Required for "trained_model" mode:
- clip_config.json
- clf.joblib

Optional:
- calibrator.joblib

If these files don't exist, the backend runs in demo_fallback mode
